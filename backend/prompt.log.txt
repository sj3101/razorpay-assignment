Create the initial project setup for a backend assignment using:

* Node.js
* Express.js
* PostgreSQL (Supabase)
* Drizzle ORM
* JWT Authentication
* HTTP-only Cookies
* bcrypt

Tech Constraints:

* JavaScript only (NO TypeScript)
* Express.js
* PostgreSQL hosted on Supabase
* Drizzle ORM
* MVC architecture

Required Architecture:

src/
├── controllers/
├── services/
├── routes/
├── middlewares/
├── config/
├── db/
│   ├── schema/
│   ├── migrations/
│   └── seed/
├── utils/
├── app.js
└── server.js

Tasks:

1. Initialize package.json.

2. Install and configure all required dependencies:

   * express
   * cors
   * dotenv
   * cookie-parser
   * bcrypt
   * jsonwebtoken
   * drizzle-orm
   * drizzle-kit
   * pg
   * nodemon

3. Configure package.json scripts exactly as:

"dev"
"db:migrate"
"db:seed-data"

4. Configure Express application:

   * JSON middleware
   * cookie-parser
   * cors
   * environment variables
   * centralized app bootstrap

5. Configure server startup:

   * Must run on port 7002
   * Read PORT from environment but default to 7002

6. Configure Supabase PostgreSQL connection using Drizzle ORM.

7. Create:

   * drizzle.config.js
   * database connection file
   * .env.example

8. Create Drizzle schemas for:

   * users
   * employee_manager
   * reimbursements

Requirements:

* Use UUID primary keys.
* Use PostgreSQL enums for roles and reimbursement statuses.
* Add createdAt and updatedAt timestamps where appropriate.
* Define foreign key relationships.

9. Create migration setup so that:

npm run db:migrate

creates all required tables.

10. Create a seed script so that:

npm run db:seed-data

creates exactly one CFO account:

email: [cfo@org.com](mailto:cfo@org.com)
password: CFO#ORG@April2026
role: CFO

Hash the password using bcrypt.

11. Do NOT use Supabase Auth.
    Authentication will later be implemented using:

* Users table
* bcrypt
* JWT
* HTTP-only cookies

12. Do NOT generate:

* Controllers implementation
* Routes implementation
* Middleware implementation
* Auth logic
* Business logic
* Reimbursement workflows

Only generate project setup, database configuration, schemas, migration setup, and CFO seed setup.

After creating everything, provide the final folder structure and list every file created.

The project setup is already complete.

Current stack:

* Node.js
* Express.js
* PostgreSQL (Supabase)
* Drizzle ORM
* JavaScript

Your task is ONLY to implement the database layer.

Requirements:

Create PostgreSQL enums:

1. role_enum

   * EMP
   * RM
   * APE
   * CFO

2. approval_status_enum

   * PENDING
   * APPROVED
   * REJECTED

Create the following Drizzle schemas:

1. users

---

Fields:

* id (UUID primary key)
* name
* email (unique)
* password
* role (role_enum)
* createdAt
* updatedAt

Constraints:

* email must be unique
* role defaults to EMP

2. employee_manager

---

Fields:

* id (UUID primary key)
* employeeId (FK -> users.id)
* managerId (FK -> users.id)
* createdAt

Constraints:

* employeeId must be unique
* employeeId and managerId cannot be the same user

Business Rule:

* Every employee reports to exactly one RM

3. reimbursements

---

Fields:

* id (UUID primary key)
* employeeId (FK -> users.id)
* title
* description
* amount (numeric/decimal)
* rmApproval (approval_status_enum)
* apeApproval (approval_status_enum)
* finalStatus (approval_status_enum)
* createdAt
* updatedAt

Defaults:

* rmApproval = PENDING
* apeApproval = PENDING
* finalStatus = PENDING

Requirements:

1. Generate complete Drizzle schema files.
2. Generate table relationships.
3. Generate migration files.
4. Generate commands required to create migrations.
5. Generate commands required to run migrations.
6. Verify that the schema is compatible with Supabase PostgreSQL.
7. Show the final folder structure after schema creation.

Do NOT generate:

* Controllers
* Routes
* Middleware
* Services
* Authentication
* Seed logic

Focus only on schema design and migration setup.

At the end provide:

1. Exact command to generate migration.
2. Exact command to apply migration.
3. Expected tables and enums that should appear in Supabase after migration succeeds.

Create the database integration layer for the existing backend project using:

Node.js
Express.js
PostgreSQL (Supabase)
Drizzle ORM
JavaScript only (NO TypeScript)

Current setup already includes:

Drizzle schema
Migration system
Supabase PostgreSQL connection

Tasks:

Create a clean database abstraction layer inside:
src/db/
Implement:
db client initialization using Drizzle
reusable query wrapper functions (CRUD helpers)
safe parameterized query handling
Create a repository pattern structure:
src/db/repositories/

for:

users repository
employee_manager repository
reimbursements repository
Each repository must include:
create
findById
findAll
update
delete
Ensure all queries use Drizzle ORM only (no raw SQL unless necessary).
Add error handling wrapper for DB operations.

Output:

Folder structure
All file contents
Explanation of repository pattern usage

Do NOT generate:

controllers
routes
auth logic
middleware


Build the service layer for the backend using MVC architecture.

Tasks:

Create service layer inside:
src/services/
Implement services for:
UserService
AuthService (logic only, no routes)
ReimbursementService
ManagerService
Each service should:
Use repository layer only (no direct DB access)
Contain business logic only
Handle validation rules
Handle role-based rules internally
Implement key business rules:
EMP can only create reimbursement
RM can approve/reject first level
APE can approve/reject second level
CFO has full access
Add reusable helper functions:
password hashing logic wrapper
token generation wrapper (JWT)
status transition validation

Output:

Full service layer code
Flow diagram (text-based)
File structure update

Do NOT generate:

routes
controllers
middleware


Create the controller layer for the backend.

Tasks:

Create controllers inside:
src/controllers/
Implement:
authController
userController
reimbursementController
managerController
Each controller must:
Call service layer only
Never directly access DB
Handle request/response only
Return consistent JSON format
Standard response format:
{
  "success": true,
  "message": "",
  "data": {}
}
Add:
input validation (basic manual validation, no external libraries unless necessary)
error handling middleware hooks (but not middleware itself)

Output:

Full controller code
API response structure
Folder mapping

Do NOT generate:

routes
middleware
services
DB Layer


Create Express routing layer for the backend.

Tasks:

Create routes inside:
src/routes/
Implement:
auth.routes.js
user.routes.js
reimbursement.routes.js
manager.routes.js
Connect:
routes → controllers only
Define REST API endpoints:

Auth:

POST /auth/register
POST /auth/login
POST /auth/logout

Users:

GET /users
GET /users/:id

Reimbursements:

POST /reimbursements
GET /reimbursements
PATCH /reimbursements/:id

Manager:

POST /manager/assign
PATCH approval endpoints
Setup:
/api/v1

base prefix

Setup centralized route loader in app.js

Output:

Full routing system
Express route structure
Endpoint list

Do NOT generate:

controllers logic
services
Middleware


Implement authentication system using:

JWT
bcrypt
HTTP-only cookies
Express.js
PostgreSQL (users table)

Tasks:

Implement AuthService logic:
register user
login user
logout user
Password handling:
bcrypt hashing
password comparison
JWT:
access token generation
token verification middleware
Cookies:
HTTP-only cookie storage
secure flag handling
Roles supported:
EMP
RM
APE
CFO
Add token expiry handling

Output:

Full auth implementation
Security explanation
Flow diagram (login/register lifecycle)

Do NOT generate:

routes (only logic layer)
controllers (if already done, just connect)
DB schemas


Create middleware system for backend security.

Tasks:

Create middleware inside:
src/middlewares/
Implement:
authMiddleware (JWT verification)
roleMiddleware (RBAC system)
errorHandler middleware
requestLogger middleware
Role-based access control:
EMP → limited access
RM → approval rights
APE → second approval
CFO → full access
Add protection for all sensitive routes
Ensure middleware is reusable and modular

Output:

Full middleware code
RBAC logic explanation
Middleware flow diagram

Do NOT generate:

controllers
services
Routes


Integrate full backend system and prepare for production readiness.

Tasks:

Connect:
routes
controllers
services
middleware
DB layer
Ensure:
app.js central bootstrap is clean
server.js starts correctly on port 7002
Add:
basic logging system
error handling standardization
API response consistency
Create test checklist:
auth flow
reimbursement flow
role flow
Provide:
full final folder structure
startup instructions
environment setup guide

Output:

Complete working backend architecture
Run instructions
Final system overview